# chatter
Chat bot


# Instructions
First, go to platform.openai.com and sign up for a free account.
Get an api key and copy the example.env file (name it ".env"). Add the api key value to the newly created .env file.



# Update Python Dependencies
To update Python dependencies (requirements.txt):
pip freeze > requirements.txt


